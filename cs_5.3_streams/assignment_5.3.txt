1. Ќаписать консольное приложение, реализующее работу с файлами.

ѕриложение должно удовлетвор€ть следующим требовани€м.

	1) наличие меню;
	2) возможность осуществл€ть поиск файлов и папок по имени, размеру, датам создани€, доступа и модификации;
	3) осуществл€ть поиск текстовых файлов по содержимому;
	4) работа с найденным списком - удаление, перемещение, копирование, замена в выбранных текстовых файлах одной подстроки на другую.

2. Ќаписать консольное приложение, принимающее на вход путь к файлу, содержащему xhtml разметку.
ѕриложение с помощью регул€рных выражений должно осуществл€ть очистку от тегов содержимого переданного файла и предлагать сохран€ть результат в другой текстовый файл.